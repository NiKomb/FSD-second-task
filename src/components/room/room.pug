include /components/button/button
include /components/rating/rating
include /components/room-slider/room-slider
include /components/card-number/card-number


mixin room(options)
  -
    const {
      form,
      srcIMG="#",
      dotIMG=1,
      images,
      href="#",
      number=1,
      luxury=false,
      price=0,
      rate=3,
      reviews=0
    } = options;

  form.room(name=`${form} № ${number} `)
    fieldset.room__field
      +room-slider({dot:true, items:images, href:href, check:dotIMG})
    fieldset.room__field
      .room__number
        +card-number({
          number:number,
          price:price,
          luxury:luxury,
          isMiddleNumber:true})
      .room__group
        +rating({title:"", rate:rate, name: `${form} № ${number} `})
        .room__reviews
          b.room__reviews-bold| #{reviews}
          |Отзывов
