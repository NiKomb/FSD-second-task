mixin toggle({text, checked = false})
  span.toggle
    label.toggle__label(class=classes)
      input.toggle__input(type="checkbox" checked=checked)
      span.toggle__box
      span.toggle__text= text
