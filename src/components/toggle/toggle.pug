mixin toggle(options={})
  -
    const {
      text="text",
      checked = false
    }=options

  span.toggle
    label.toggle__label
      input.toggle__input(type="checkbox" checked=checked)
      span.toggle__box
      span.toggle__text= text
