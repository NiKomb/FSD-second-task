mixin radio(options={})
  -
    const {
      text="text",
      name="radio",
      value='male',
      checked=false
    }=options

  span.radio
    label.radio__label
      input.radio__input(type="radio" name=name value=value checked=checked)
      span.radio__box
      span.radio__text= text
