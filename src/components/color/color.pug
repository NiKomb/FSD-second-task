mixin color(colors = [])
  .color
    each item in colors
      .color__item
        .color__box(class= `color__box_theme_${item.theme}`)
        .color__info
          p.color__text= item.text
          p.color__code= item.code
